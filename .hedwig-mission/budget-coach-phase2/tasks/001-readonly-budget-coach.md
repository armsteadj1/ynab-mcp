# Task 001 — Read-only budget coach tools

Implement the first read-only budget coach layer.

Add tools:

- `get_budget_planning_snapshot`
- `find_months_needing_budget_help`
- `explain_overspending`
- `get_categorization_queue`
- `suggest_transaction_categories`
- update/replace weekly/monthly review behavior to match the operating model

Use live YNAB client reads. Do not require local sync data. Keep all YNAB mutations out of scope.
